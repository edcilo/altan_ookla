CREATE TABLE IF NOT EXISTS stnet (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    test_id BIGINT NULL,
    test_date TIMESTAMP NULL,
    client_ip_id BIGINT NULL,
    client_ip_address_s VARCHAR(20) NULL,
    download_kbps INT NULL,
    upload_kbps INT NULL,
    latency SMALLINT NULL,
    server_name VARCHAR(120) NULL,
    server_country VARCHAR(120) NULL,
    server_country_code VARCHAR(10) NULL,
    server_latitude FLOAT NULL,
    server_longitude FLOAT NULL,
    server_sponsor_name VARCHAR(120) NULL,
    user_agent VARCHAR(255) NULL,
    isp_name VARCHAR(120) NULL,
    is_isp SMALLINT NULL,
    connection_type SMALLINT NULL,
    client_country VARCHAR(120) NULL,
    client_country_code VARCHAR(10) NULL,
    client_region_name VARCHAR(120) NULL,
    client_region_code VARCHAR(10) NULL,
    client_city VARCHAR(120) NULL,
    client_latitude FLOAT NULL,
    client_longitude FLOAT NULL,
    miles_between FLOAT NULL,
    gmaps_formatted_address VARCHAR(120) NULL,
    gmaps_name VARCHAR(120) NULL,
    gmaps_type VARCHAR(120) NULL,
    gmaps_country VARCHAR(120) NULL,
    gmaps_country_code VARCHAR(10) NULL,
    gmaps_region VARCHAR(120) NULL,
    gmaps_subregion VARCHAR(120) NULL,
    gmaps_subsubregion VARCHAR(120) NULL,
    gmaps_postal_code VARCHAR(10) NULL,
    session_id_a BIGINT NULL,
    test_method_a VARCHAR(120) NULL,
    test_source_a VARCHAR(120) NULL,
    client_ipv6_address VARCHAR(120) NULL,
    jitter_a INT NULL,
    timezone_name_a VARCHAR(120) NULL,
    km_between FLOAT NULL,
    server_selection_a VARCHAR(120) NULL,
    timezone_offset_seconds_a INT NULL,
    isp_common_name_a VARCHAR(120) NULL,
    download_server_count_a SMALLINT NULL
);
